# Staging

Collection packets write findings here as `<packet-id>.yaml`, one record per node, in the schema
defined in [`../COLLECTION-PROTOCOL.md`](../COLLECTION-PROTOCOL.md) §1.

Nothing here is authoritative. Records merge into each śākhā's `_status.yaml` in a single
reviewed pass — never by the collecting agent, so parallel packets cannot collide.
