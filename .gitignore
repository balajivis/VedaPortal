node_modules/
.next/
.env
.DS_Store
*.log
node_modules

# Audio never lives in git — object storage + _recordings.yaml manifests only
# (see recordings/README.md). No git-lfs either.
*.wav
*.flac
*.mp3
*.m4a
*.aac
*.ogg
*.opus
*.aiff
*.wma
*.mp4
*.mov
*.mkv
.gstack/

# Kashyap / SAKSI edition — retained locally for reading, never committed.
# Publisher permission is student-printout-only; file is RC4-encrypted (copy:no).
# Clean accented replacements: VedaWeb eichler.csv, Itranslator-2003, sanskritdocuments.org
sources/vedas/rigveda/shakala/samhita/Entire Rig Veda Samhita.pdf

# Fetched source scans — large PD page-image PDFs. Files stay local;
# git tracks sources/_fetched/_manifest.yaml only (same pattern as recordings/).
sources/_fetched/*
!sources/_fetched/_manifest.yaml
!sources/_fetched/README.md

# Commentary staging. Agents write one record per sūkta here and the merge
# step folds them into commentary-mandala-N.json; the loose files are
# intermediate and several agents write concurrently.
sources/vedas/rigveda/shakala/samhita/commentary/_pending/

# TypeScript incremental build cache — regenerated on every typecheck
*.tsbuildinfo
